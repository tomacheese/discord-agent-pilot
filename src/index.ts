/**
 * Entry point for discord-agent-pilot. Replaced with the real bootstrap
 * sequence in a later task (config load → DB init → Discord client →
 * orchestrator).
 */
console.log('discord-agent-pilot starting...')
