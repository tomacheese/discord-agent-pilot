# esbuild is a transitive build dependency of tsx (dependencies) and
# vitest/vite (devDependencies); its postinstall script copies the
# platform-specific native binary into place, so it must be allowed to run.
allowBuilds:
  esbuild: true
